---
import HeaderNav from "@/components/HeaderNav.astro";
import HeaderMeta from "@/components/HeaderMeta.astro";
import { SITE_TITLE, SITE_DESCRIPTION, CDN_URL, ESO_API_URL } from "@/consts";
import Footer from "@/components/Footer.astro";
import ScrollToTop from "@/components/ScrollToTop.astro";
import { md2html } from "@/utils";

const query = new URLSearchParams();
query.set("sort[0]", "id:asc");
query.set("populate", "*");

const response = await fetch(`${ESO_API_URL}/api/worldevents?${query}`);
const result = await response.json();
const data = result?.data;
---

<html lang="zh-CN">
  <head>
    <HeaderMeta
      title={`世界事件 - 上古卷轴OL攻略 - ${SITE_TITLE}`}
      description={SITE_DESCRIPTION}
    />
    <link rel="stylesheet" href={`${CDN_URL}/assets/gfm-fba346.css`} />
  </head>

  <body>
    <HeaderNav />
    <main class="max-w-screen-xl mx-auto grid grid-cols-1 md:grid-cols-4">
      <article class="p-6 md:col-span-3">
        <div class="markdown-body">
          <h1>
            世界事件
            <small class="text-gray-500">World Event</small>
          </h1>
          <p>
            世界事件，也称为入侵，是在每个区域的固定位置定期发生的大规模活动。它们被设计为最困难的陆上内容类型，适用于四人或四人以上的团体。
          </p>
          <p>
            上古卷轴OL一共有 {data.length} 种世界事件:
          </p>

          <ul>
            {
              data.map((x: any) => (
                <li>
                  <Fragment
                    set:html={md2html(
                      `[${x.attributes.name}](/worldevent/${x.attributes.slug}) - ${x.attributes.description}`
                    )}
                  />
                </li>
              ))
            }
          </ul>
        </div>
      </article>
    </main>
    <Footer />
    <ScrollToTop />
  </body>
</html>
